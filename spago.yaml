package:
  dependencies:
    - datetime
    - effect
    - forgetmenot
    - functions
    - nullable
    - prelude
    - react-basic
    - react-basic-hooks
    - record
    - type-equality
    - typelevel-prelude
    - unsafe-coerce
    - web-dom
    - web-events
    - web-html
    - web-touchevents
  name: react-basic-dom-beta
  test:
    main: Test.Main
    dependencies:
      - console
      - spec
      - node-fs
      - aff
      - node-buffer
      - foldable-traversable
      - tuples
  # Optional section for configuring the `spago publish` command.
  # If you intend to publish your package, this section becomes mandatory.
  publish:
    # The version of your package. This follows semver rules, but with no
    # prereleases - so only major.minor.patch.
    version: 0.1.0
    license: WTFPL
    location:
      githubOwner: rowtype-yoga
      githubRepo: purescript-react-basic-dom-beta
workspace:
  extraPackages: {}
  packageSet:
    registry: 51.2.0
